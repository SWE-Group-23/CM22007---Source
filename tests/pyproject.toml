[project]
name = "tests"
version = "0.1.0"
description = "Runs integration and end-to-end tests."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pika>=1.3.2",
    "requests>=2.32.3",
    "scylla-driver>=3.28.2",
    "valkey>=6.1.0",
]
