apiVersion: apps/v1
kind: Deployment
metadata:
  name: create-profile
  namespace: profile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: create-profile
  template:
    metadata:
      labels:
        app: create-profile
    spec:
      containers:
        - name: create-profile
          image: create-profile:latest
          imagePullPolicy: IfNotPresent
          tty: true
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /home/user1/.cache/uv
              name: uv
            - mountPath: /tmp
              name: temp
          env:
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: create-profile-rabbitmq-user-user-credentials
                  key: username
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: create-profile-rabbitmq-user-user-credentials
                  key: password
            - name: SCYLLADB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: profile-user-scylla-creds
                  key: username
            - name: SCYLLADB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: profile-user-scylla-creds
                  key: password
            - name: SCYLLADB_KEYSPACE
              valueFrom:
                configMapKeyRef:
                  name: profile-keyspace
                  key: keyspace
      volumes:
        - emptyDir: {}
          name: uv
        - emptyDir: {}
          name: temp

---

apiVersion: rabbitmq.com/v1beta1
kind: User
metadata:
  name: create-profile-rabbitmq-user
  namespace: profile
spec:
  tags:
  - policymaker
  rabbitmqClusterReference:
    name: rabbitmq
    namespace: rabbitmq

---

apiVersion: rabbitmq.com/v1beta1
kind: Permission
metadata:
  name: create-profile-rabbitmq-permission
  namespace: profile
spec:
  vhost: "/"
  userReference:
    name: "create-profile-rabbitmq-user"
  permissions:
    write: "create-profile-rpc-resp-exc"
    configure: ""
    read: "create-profile-rpc-call-q"
  rabbitmqClusterReference:
    name: rabbitmq
    namespace: rabbitmq
